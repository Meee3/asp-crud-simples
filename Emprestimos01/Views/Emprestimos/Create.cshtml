@model Emprestimos01.Models.Emprestimos

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Emprestimos</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="valorSolicitado" class="control-label"></label>
                <input asp-for="valorSolicitado" class="form-control" />
                <span asp-validation-for="valorSolicitado" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="valorEntrada" class="control-label"></label>
                <input asp-for="valorEntrada" class="form-control" />
                <span asp-validation-for="valorEntrada" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
